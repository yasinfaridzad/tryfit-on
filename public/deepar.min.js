// Minimal fake deepar for testing (real SDK muss extra geladen werden!)
const deepar = {
    version: "1.0.0-fake",
    initialize: async ({ canvas, licenseKey, effect, additionalOptions }) => {
      console.log("DeepAR initialized with licenseKey: ", licenseKey);
      console.log("Effect Loaded:", effect);
      const context = canvas.getContext('2d');
      context.fillStyle = 'lightgrey';
      context.fillRect(0, 0, canvas.width, canvas.height);
      return {
        callbacks: {},
      };
    },
  };
  